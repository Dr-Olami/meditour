---
import Layout from '../../layouts/Layout.astro';
import { Navbar } from '../../design-system/components/organisms/Navbar';
import { Footer } from '../../design-system/components/organisms/Footer';
import { Icon } from '../../design-system/components/atoms/Icon';
import Breadcrumb from '../../design-system/components/molecules/Breadcrumb.astro';
import { getTranslations } from '../../lib/i18n';
import { getNavLinks } from '../../lib/navigation';
import { getFooterLegalLinks } from '../../lib/footer';
import { medicalBusiness } from '../../lib/schema';
import {
  getCountriesGroupedByRegion,
  getRegionLabel,
  getCountryCount,
} from '../../data/countries';
import type { Region } from '../../data/countries';

const locale = 'en';
const t = getTranslations(locale);

const countriesByRegion = getCountriesGroupedByRegion();
const totalCountries = getCountryCount();

const siteUrl = (Astro.site ? Astro.site.toString() : Astro.url.origin).replace(/\/$/, '');
const whatsappNumber = import.meta.env.PUBLIC_WHATSAPP_NUMBER || '8801611892986';
const contactEmail = import.meta.env.PUBLIC_CONTACT_EMAIL || 'khan@meditour.com';

const jsonLd = medicalBusiness({
  name: t.site.name,
  url: siteUrl,
  whatsapp: whatsappNumber,
  email: contactEmail,
});
---

<Layout
  title={`Countries We Serve | ${t.site.name}`}
  description={`Khan Meditour serves patients from ${totalCountries} countries across South Asia, Middle East, Africa, and Western nations. Find country-specific medical tourism guidance, cost comparisons, and visa information.`}
  locale={locale}
  jsonLd={jsonLd}
>
  <Navbar
    brand={t.site.name}
    links={getNavLinks(locale)}
    cta={{ label: t.nav.cta, href: '#contact' }}
    locale={locale}
    currentPath={Astro.url.pathname}
    client:load
  />

  <main>
    {/* ── HERO ─────────────────────────────────────────────────────────── */}
    <section class="relative overflow-hidden bg-cream-100 text-ink">
      <div class="pointer-events-none absolute -top-40 right-0 h-[30rem] w-[30rem] rounded-full bg-violet-600/10 blur-3xl" aria-hidden="true" />
      <div class="pointer-events-none absolute -bottom-48 -left-24 h-[26rem] w-[26rem] rounded-full bg-indigo-600/5 blur-3xl" aria-hidden="true" />

      <div class="container relative pb-16 pt-28 md:pb-20 md:pt-36">
        <Breadcrumb
          items={[{ label: t.nav.home, href: '/' }, { label: 'Countries' }]}
          class="mb-10"
          data-anim="fade-in-up"
        />

        <div data-anim="stagger-children">
          <span class="inline-block rounded-full bg-gradient-accent px-3 py-1 text-xs font-semibold text-white">
            {totalCountries} Countries
          </span>

          <h1 class="mt-4 max-w-3xl font-display text-4xl font-bold leading-[1.05] tracking-tight text-ink md:text-5xl" data-anim="headline-reveal">
            Countries we serve
          </h1>

          <p class="mt-4 max-w-2xl text-lg leading-relaxed text-ink/70">
            Khan Meditour serves patients from {totalCountries} countries across South Asia, the Middle East, Africa, and Western nations. Find country-specific medical tourism guidance, cost comparisons, visa information, and patient stories.
          </p>
        </div>
      </div>
    </section>

    {/* ── COUNTRY DIRECTORY ────────────────────────────────────────────── */}
    <section class="bg-cream-200 py-16 md:py-20">
      <div class="container">
        {(Object.entries(countriesByRegion) as [Region, typeof countriesByRegion[Region]][0][]).map(([region, countries]) => (
          <div class="mb-12 last:mb-0" data-anim="scroll-reveal">
            <h2 class="mb-6 font-display text-2xl font-bold text-ink md:text-3xl">
              {getRegionLabel(region)}
            </h2>
            <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
              {countries.map((country) => (
                <a
                  href={`/for/${country.slug}`}
                  class="group flex flex-col rounded-card border border-cream-300 bg-cream-100 p-5 transition-all hover:border-violet-400 hover:shadow-base"
                >
                  <div class="flex items-center justify-between">
                    <span class="text-xs font-semibold text-ink/50">{country.code}</span>
                    <Icon name="arrow-right" size={16} className="text-ink/40 transition-colors group-hover:text-violet-600" />
                  </div>
                  <h3 class="mt-2 font-display text-lg font-semibold text-ink">{country.name}</h3>
                  <p class="mt-1 text-sm text-ink/60">
                    Save {country.costSavingsVsLocal} · {country.flightTime}
                  </p>
                  <div class="mt-3 flex flex-wrap gap-1">
                    {country.languages.slice(0, 2).map((lang) => (
                      <span class="rounded-full bg-cream-200 px-2 py-0.5 text-xs text-ink/60">{lang}</span>
                    ))}
                  </div>
                </a>
              ))}
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* ── CTA ──────────────────────────────────────────────────────────── */}
    <section id="contact" class="bg-ink py-16 text-cream-100 md:py-20" data-anim="fade-in-up">
      <div class="container text-center">
        <h2 class="font-display text-2xl font-bold text-cream-100 md:text-3xl">
          Don't see your country?
        </h2>
        <p class="mx-auto mt-4 max-w-xl text-cream-100/60">
          We help patients from around the world. Contact us for personalized medical tourism guidance regardless of your location.
        </p>
        <div class="mt-8 flex flex-wrap justify-center gap-3">
          <a
            href="/second-opinion"
            class="inline-flex items-center gap-2 rounded-card bg-gradient-accent px-6 py-3 text-sm font-semibold text-white"
          >
            Get Free Second Opinion
          </a>
          <a
            href={`https://wa.me/${whatsappNumber}`}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 rounded-card border border-cream-100/30 px-6 py-3 text-sm font-semibold text-cream-100 transition-colors hover:bg-cream-100 hover:text-ink"
          >
            <Icon name="whatsapp" size={18} />
            Chat on WhatsApp
          </a>
        </div>
      </div>
    </section>
  </main>

  <Footer
    brand={t.site.name}
    legalLinks={getFooterLegalLinks(locale)}
    conciergeLabel={t.footer.concierge}
    conciergePrefix={t.footer.conciergePrefix}
    conciergeSuffix={t.footer.conciergeSuffix}
    copyright={`© ${new Date().getFullYear()} ${t.site.name}. ${t.footer.rights}`}
    address={t.footer.address}
  />
</Layout>
